# 技能墓园

**中文** · [English](README.en.md)

← [技能目录](../README.md)

被删掉的技能埋在这里。每块碑记四件事：生卒、生平、死因、遗产。碑文只写查得到的事实，生年按入库算——入库之前它们在本地活过多久，已不可考。

删一个技能时顺手立一块碑，按删除时间倒序排在最上面。

---

## `alear030-worktree-change-guard`

**2026-08-19 — 2026-09-23 · 入库后 35 天**

**生平**：在 worktree 里改完生产代码之后，回读目标 worktree 的绝对路径、核对 diff。防的是 Edit 的改动落进主仓库、worktree 纹丝不动、测试一直崩这类怪事。一生都是一条强建议，从没有 hook 替它托底。

**死因**：一是病因始终没查清，它只能要求结果、讲不出原因；二是它开的药方——用 Python 把整个文件覆盖写回 worktree——出错时的代价比它要治的病还大。

**遗产**：CLAUDE.md「反直觉陷阱」里的一行：在 worktree 里改文件，用 worktree 自己的路径。

---

## `alear030-multitask-pipeline`

**2026-08-19 — 2026-09-11 · 入库后 23 天**

**生平**：从别的项目迁入，负责把大任务拆开并行派发。

**死因**：129 场会话里一次都没被调用过。临终体检还查出三处与现场矛盾：引用的 `alear-executor` 子代理类型并不存在；声称 `AGENTS.md` 是本地文件，实际它一直被 Git 跟踪。

**遗产**：没有。它引用的 `alear-executor` 在 2026-09-23 也从 CLAUDE.md 里抹去了，算是同穴而眠。

---

## `alear030-multitask-code`

**2026-08-19 — 2026-09-11 · 入库后 23 天**

**生平**：`multitask-pipeline` 的同胞，管并行改代码时的写法纪律。

**死因**：同样零调用；它把写法的权威来源指向 `.cursor/rules/*.mdc`，而 Claude Code 并不会自动加载那里。

**遗产**：「先预览后落盘、不顺手重构」被内联进 `alear030-issue-fix`，多活了十二天；2026-09-23 改写为按 CLAUDE.md「改动范围」界定。
