---
name: alear030-clear-logdata
description: "清理 Alear030 没有实际对话意义的会话过程文件时使用：测试启动、打招呼试连通、开了就关的空会话，在 session_detail、trace_log、log_data 等处按 session_id 各留一份。按 session 归集全部产物、读用户原话判断有无实质、排除被 memory 引用与可能正在运行的会话，列清单交用户逐条确认后整组移入隔离目录，可按批还原。用户说清理测试会话、清空会话、删没用的 session/trace/log 记录时用这个 skill。"
---

# Alear030 会话过程文件清理

一次会话会在多个目录各落一份以 `session_id` 为文件名的过程文件，清理的单位是**会话**而不是文件：判断一次，整组处理，不会只删了 detail 漏掉 trace。

这些目录全部被 git 忽略，删了无法从仓库恢复，所以硬边界是：**只清用户逐条确认过的 session，一律移进隔离目录，不做任何删除。** 不用系统回收站——文件放不进回收站时 shell 会改为永久删除，而为了不弹窗压掉确认框，这一步就会静默发生。按 session_id 聚合的共享文件（memory 存储、memory 管线日志）里的行不在清理范围内。

## 工具

`logdata.py` 与本文件同目录，从要清理的那个 checkout 根目录调用（它按当前目录的 git 仓库定位数据）：

- `python <skill 目录>/logdata.py scan`：只读，输出 JSON
- `python <skill 目录>/logdata.py quarantine <id> [<id> ...]`：重扫后把这些 session 的全部产物按原相对路径移进隔离目录的一个新批次（`scan` 输出的 `quarantine_dir` 下，按 checkout 分开），附 manifest。检测到有 Alear030 会话进程在运行（`main.py` 或 `alear030` 命令），或拿不到进程列表时，整批拒绝执行
- `python <skill 目录>/logdata.py restore <批次>`：遍历批次目录按相对路径移回原处；批次名必须是隔离目录下的时间戳批次，原处已有同名文件的不覆盖，报失败，处理后重跑即可

发现规则不维护目录清单，靠项目约定「单 session 过程数据以 session_id 为文件名」在所有被忽略的目录里找，再只保留**按会话存数据的目录**：至少两个文件、且过半文件能对上现有 session_detail。新加的过程文件只要沿用这条约定就不用改脚本；不沿用的（按会话建子目录、文件名带前后缀）会被漏掉，刚出现、只有一个文件的目录也暂时不算——这两种都只会漏清，不会误清。不达标目录里形如 session_id 的文件（如 plan 模式遗留的 session_plan、无关目录里的撞名文件）进 `unkeyed_files`，脚本永远不移动它们。

scan 输出里的 `artifact_dirs` 是这次认定的会话目录，出现没见过的目录时在报告里点出来。session_detail 下只要有一个文件不符合 `<session_id>.json`，脚本就报错退出，那是约定变了，停下来报告，不要改正则绕过去。

## 流程

分两段，中间必须停。**调用这个技能本身不是清理授权**，第一段结束时这一轮就结束。

**第一段：圈选 + 理由汇报（只读）**

1. **确定范围。** 主仓库和各 worktree 的数据互相独立，`git worktree list` 列出全部 checkout。用户点名了就只扫那个；没点名就每个都 scan（只读无副作用），分 checkout 汇报。
2. **scan**，按下面的分类逐个 session 圈选，每个圈进来的 session 都要写出理由。
3. **按清单格式汇报，停下。** 不调用 `quarantine`，不追问「要不要现在清」之外的任何动作。

**第二段：执行（只在用户给出决策之后）**

4. 用户的决策要能落到具体 id 和 checkout 上：可以增删；「按建议来」指建议清理表里的 id，单列区不含在内；说不清清哪些就再问，不按自己的圈选推断。
5. **quarantine** 这些 id，如实汇报批次名、每个 session 的结果，`partial`、`skipped_*`、`not_found` 单独说明，并告诉用户还原命令。隔离目录不会自动清空，占用的空间由用户自己决定何时处理。

## 怎么判断

判断依据是**用户原话有没有实质内容**，不是轮数。`user_excerpts` 只截了前 80 字，拿不准就去读那份 detail。`user_messages` 按落盘角色计数，系统注入的收尾提示、早期被当作 user 落盘的 system-reminder 也算在内，所以它只会偏多：看到摘录不像用户说的话，以 detail 原文为准。

| 情况 | 建议 | 说明 |
|------|------|------|
| `maybe_active` | 跳过，不进清单 | 最近 10 分钟有写入。闲置中的会话不写文件、这条判不出来，由 `running_sessions` 兜底：非空就在摘要里点出，请用户先关掉会话，否则第二段会被拒绝 |
| 有 detail，用户消息为 0 | 清 | 启动即退出的空壳 |
| 原话只是打招呼、「测试一下 xx」、确认某功能连没连上 | 清 | 附上原话，让用户一眼能核 |
| 轮数少但在讨论真实问题 | 保留，不进建议 | 一句话的会话也可能有价值 |
| `memory_referenced` | 单列，不建议清 | memory 里的来源会指向不存在的 session。用户明确坚持时才加 `--allow-memory-referenced` |
| 没有 detail（只有其他产物） | 单列，不判 | 无法从对话判断，可能是下线功能留下的独立数据而非会话垃圾，列出所在目录交用户决定 |
| `detail_error` | 单列，不判 | 空文件或结构不符，本身是值得告诉用户的发现 |
| `unkeyed_files` | 单列，不在清理范围 | 列出所在目录与文件数，说明它们不按会话存数据；要处理由用户自己决定，脚本不代劳 |

没开 memory pipeline 的会话不等于没意义——有实质内容的照样保留，用户可能想补跑管线。

## 清单格式（中文）

1. 摘要：checkout 路径、扫到的 session 数、认定的会话目录、正在运行的会话进程、建议清理数、单列数。
2. 建议清理表：session_id / 用户原话摘录 / 产物（目录名 + 大小）/ 理由。
3. 单列区：memory 引用、无 detail、损坏、`unkeyed_files` 各自成组，说明为什么不建议或不判。
4. 末尾明确请用户确认要清理的 id。
