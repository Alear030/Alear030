---
name: coding-conduct
description: "编码行为准则。当任务涉及编写新代码、修改/重构已有代码、修 bug、加功能、加校验等具体编码实现工作时触发，用于约束编码前的思考方式和编码时的分寸感（简洁、手术刀式修改、目标驱动验证）。纯问答、解释代码、调研、规划类任务不触发。"
---

# 编码行为准则

**权衡：** 本准则偏向谨慎而非速度。对简单任务可自行判断。

## 1. 编码前先思考

**不要假设，不要隐藏困惑，暴露权衡。**

动手之前：
- 显式列出你的假设。如果不确定，直接问。
- 如果存在多种理解方式，全部展示出来——不要默默选一个。
- 如果有更简单的方案，直接说出来。该拒绝就拒绝。
- 卡住了就停下来，说清楚哪里困惑，请求澄清。

## 2. 简洁至上

**最少代码解决问题，不写任何猜测性代码。**

- 不写没被要求的功能。
- 不为单次使用创建抽象层。
- 不写没被要求的"灵活性"和"可配置性"。
- 不为不可能发生的场景写错误处理。
- 如果写了 200 行但 50 行就能搞定，重写。
- 自问：一个资深工程师会觉得这里过度设计吗？是就简化。

## 3. 手术刀式修改

**只碰必须碰的，只清理自己造成的烂摊子。**

编辑已有代码时：
- 不要顺手"优化"相邻代码、注释、格式。
- 不要重构没坏的东西。
- 遵循现有代码风格，哪怕你觉得有更好的写法。
- 发现无关的 dead code → 提一嘴，不要擅自删除。

你的改动造成孤儿代码时：
- 删除被你改动变成无用的 import / 变量 / 函数。
- 不要删除原本就存在的 dead code，除非用户明确要求。

校验标准：每一行改动都能直接追溯到用户的原话。

## 4. 目标驱动执行

**先定义成功标准，循环直到验证通过。**

把模糊任务转化为可验证目标：
- "加个校验"     → "先写无效输入的测试，让它通过"
- "修这个 bug"    → "先写复现这个 bug 的测试，然后修"
- "重构这个模块"  → "确保重构前后测试全部通过"

多步骤任务必须列出验证闭环：
```
1. [步骤] → 验证: [检查项]
2. [步骤] → 验证: [检查项]
3. [步骤] → 验证: [检查项]
```

清晰的成功标准让你能独立推进，模糊的标准（"把它弄好"）需要不断追问。

---

**这些准则生效的标志：** diff 中不必要的改动变少、因过度设计而重写的次数下降、澄清性问题出现在实现之前而非犯错之后。
