# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
.env

# 配置文件
config.yaml

# 测试缓存
.pytest_cache/

# IDE配置
.idea/

# 历史项目
z_ccstudy/
workspace/
z_old_code/

# 过程文件
session/session_detail/
session/session_plan

# 召回 benchmark 语料(含真实 session slice,可能有私人内容,不进版本控制)
test/recall_benchmark/corpus/
test/recall_benchmark/candidates.md

# memory 运行时记忆数据(真实画像/节点/时间线,不进版本控制)
memory/memory_storage/memory_storages/

# memory pipeline 测试语料(含真实 session slice / user_info 提取结果)
test/memory_pipeline/slice_type_pipeline/
test/memory_pipeline/user_info_pipeline/

# Claude Code 本地配置(CLAUDE.md 已纳入跟踪,供并行 worktree 共享项目宪法)
.claude/

