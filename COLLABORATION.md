# 我和 agent 的协作方式

**中文** · [English](COLLABORATION.en.md)

← [返回 README](README.md) · [参与方式](CONTRIBUTING.md) · [文档目录](docs/index.md)

这份文件回答的是「我和agent协同开发的历程」。仓库里其他文档讲的都是 Alear030 本身——它有什么机制、为什么长成这样；这一篇讲的是我和几个 coding agent 之间发生了什么，怎么分工，怎么协作，以及代码的哪些部分出自谁手。

---

## 目录

- [为什么写这份文件](#为什么写这份文件)
- [起步：那时候一行都不让别人写](#起步那时候一行都不让别人写)
- [转折：单人开发撞上了复杂度的上限](#转折单人开发撞上了复杂度的上限)
- [现在的边界：哪些是我写的，哪些不是](#现在的边界哪些是我写的哪些不是)
- [为什么能做到分工？](#为什么能做到分工)
- [用了哪些 agent](#用了哪些-agent)
- [代码里那些 @claude / @codex / @cursor 标记](#代码里那些-claude--codex--cursor-标记)
- [规约文件：为什么这些文件这么重要？](#规约文件为什么这些文件这么重要)
- [关于技能](#关于技能)
- [协作的节奏](#协作的节奏)
- [我怎么确认它写的东西是对的](#我怎么确认它写的东西是对的)
- [所以该怎么看](#所以该怎么看)

---

## 为什么写这份文件

我在上传了README之后，从头读了一遍，发现我没读出来一个事实，就是Alear030并非是由我自己一个人写出每一行代码。

或者说不完全是，也不完全不是。

真实情况是有边界的：**核心框架是我自己写的，外围有相当一部分不是。** 这个边界不是我随便计划出来的，它是被项目的复杂度和个人掌控力之间的矛盾一点一点逼出来的，而且到今天还在演变。

我不想让人误会，所以主动说清楚，实际上我也不想在我的第一个开源项目装作我这个人有多厉害，我想尽可能的展现这个项目本身。而且与其等哪天有人翻 commit 记录自己看出端倪（ps：我不觉得有人会翻别人的commit...我就不会，但是还是讲一讲吧），同时我也将各种我和agent之间的协作方式和Alear030配套的技能同时开源了出来

这个过程本身其实也挺有意思的，充斥着个人边界和实际工程难度的妥协，比「全是我写的」或者「都是 AI 写的」这两个答案都有意思，接下来我来回忆一下。

PS：顺便提一下我的背景，大学软工出身，算法竞赛边角料选手，毕业干了3年产品经理（是的，投敌了哈哈哈哈030） 6 月 5 号开始学 Python，6 月 11 号建的这个仓库。下面讲的所有事情都发生在这个背景下，这点得先说明，不然有些选择会让我看起来很蠢。

---

## 起步：那时候一行都不让别人写

hermes一开始陪伴了我很久，不可否认的是我的Alear030有他的影子，不是说代码，而是一些思路，比如skill的提炼实际上是有受到启发的，很好的一套harness。

它当时承担的不是写代码的角色，是**指引与教导**——教我 Python 怎么写、agent 开发是怎么回事，还有其他领域的一些知识。我们之间度过了很多，说实话没有那段我大概率走不到今天。

具体的协作方式是这样的：**我有一个想法，我不知道代码怎么实现，它给我写示例，我看，我理解，然后我自己把对应的东西写进 Alear030。**

没让他直接写的原因其实挺不好解释，直说就是我那时候对Alear030有着非常强烈的占有欲和代码洁癖。具体强烈到什么程度——hermes 有时候不小心直接往文件里写进去一两行代码，我都会直接让他帮我 `git` 还原回去。哪怕那次改动里有些我自己写的东西没保存下来、要整段重写一遍，我也要这样做。

现在回头看有点。。。说不上来的感觉，但当时确实就是这样：**我想的是Alear030每一个地方，都应该出自我自己之手**

那段时间的产出速度不高，但是不得不说，是那段时间让我深刻的理解了agent开发的各种核心模块。

6 月整月只有 7 个 commit，7 月是 54 个，8 月是 64 个。这个曲线不是我编程能力越来越厉害，主要是分工变了，以及后续其实很少是大模块的0->1开荒或者重构了。

---

## 转折：单人开发撞上了复杂度的上限

核心引擎模块完成后，Alear030的工程量和复杂程度逐渐的超出了我个人所能掌控的范围。

变大之后出现一个我一开始没预料到的问题：**一个地方的改动，往往跨过很多个模块。有的时候写两行代码我可能都要翻很多文件**

举个真实的例子。切片的逻辑是挂载到session模块下的，那个时候没有memory这个模块（这都是tech-debt，后续memory得重构一次），所以天然会需要loop的状态以及多线程的逻辑，同时切片最开始实际上没有隔离线程，会阻塞输入，体感很差，甚至本地embedding模型都是初始阻塞加载。要把它挪到后台，动的不只是 `session loop local_model`这几个模块。这种改动我一个人拿着，得同时在脑子里装下好几个模块的状态。

那个时候其实就已经开始变得很艰难了，更别说后续的memory模块，可以称得上是灾难了。但这也是我个人工程能力的上限。

于此同时另一件事情也在发生：**我对与 agent 协作的掌控能力在变强。** 我开始知道什么活能交出去、怎么交出去、交出去要给多少上下文、它做完之后我该验什么。

这个能力早期是没有的——早期我连它写的雷霆变量名称我看着都费劲，那当然只能自己写。

两件事撞在一起，就变成这样子了。。。

我想说清楚的是：**agent写Alear030的代码，这完全是我没办法被逼的，到最后我只是不想了，而非想通的。** 不是我某天觉得「让 AI 写代码是更先进的开发方式」然后主动放开的，是我先撞了墙，然后发现自己也能驾驭得住了。

顺序大概就是这个顺序，有点久远的事情了。

---

## 现在的边界：哪些是我写的，哪些不是

| 部分 | 谁落的笔 |
|------|----------|
| `loop/` `session/` `agent/` `memory/` `tui/`，以及 `tool/` 的主体 | 我自己写 |
| `hook/hook_core.py` | agent 写的，我照着抄了一遍，因为他太重要了 |
| `tool/tools/command/security.py` | agent 写的实现，命令这里太复杂了也太危险了，甚至我一开始只敢放出去白名单 |
| `local_model/` `mcp_client/` | agent 接入，我一行都没写 |
| 边角料，以及复杂但机械的数据处理（比如 JSON 的正则提取） | agent |

### 为什么核心不外包

有两点原因吧，第一点大家都能想到，第二点我觉得更重要。

**第一，我觉得工程师不应该丢掉对系统的掌控力。** 

诚然，agent现在写代码解决问题的能力已经非常强了，但是不知道是不是我个人使用agent的能力问题还是怎么，我总是觉得在横跨各大模块并且建立框架一致性的问题上，agent总是给我一种力不从心的感觉，无论是哪个agent，他们太着急于解决用户给他们的那个具体的问题了，而忽略了一个项目在宏观框架上需要做出的取舍。

同时，核心逻辑如果不在我脑子里，我后面所有的架构判断就都没有依据了。我没法判断一个方案是不是收口的、会不会和别的机制打架、值不值得为它开一条新路径——因为我根本不知道现有的路径长什么样。到那个时候我就很难说我是这个项目的作者了，可能就变成我平时工作的角色——产品经理（哈哈哈哈哈哈），知其然不知其所以然。

**第二，我很多灵感是写代码写着写着冒出来的。**

这条听起来玄，但对我是实实在在的。很多的地方都是这么出来的，甚至是记忆模块从我的想法到落地，以及那种宏大叙事和现实工程问题上的权衡和妥协能力。

比如Alear030的记忆模块，一开始想的挺好，图、树、森林，树内节点建联，树间节点建联，根节点网络之类听起来开好像挺像那么回事的，但是当我打开IDE之后，我发现这就是一场灾难，我发现这个时候如果我不进行变通，那就变成了为了写一个雷霆大数据结构算法而写的情况了，但是应该这样么？

所以对我来说，**把核心模块外包出去，等于把产生想法的那个环节，以及自我约束器，也外包了。** 这是我不愿意付的代价，也是我付不起的代价，agent会完成你的任务，但当你固执的决定时，他也只会想方设法的从你的想法出发，尝试完成你的任务，完成那个或许本身就不应该开始的“任务”。

### hook_core 这个中间态

`hook/hook_core.py` 是 agent 写的，但它对这个项目太重要了——整个事件驱动的调度都压在上面。

我的处理方式是：**照着抄了一遍。**

但我发现抄完之后，你也很难说这里的逻辑是真的进脑子了，像是`trigger` 怎么匹配、后台钩子怎么进 `_pending`、`wait_all` 退出时在等什么，这些如果让我重写一遍，我还是得看现在的代码都是怎么写的。

成本很高。

### command 的安全层：我定模型，agent 落实现

`tool/tools/command/security.py` 有 1605 行，是 agent 写给我的单个文件里最大的一块——整个 `tool/` 模块 3708 行，占了四成多。

最早那版是白名单准入：不在知识库里的命令不许执行。听起来很安全，实际两头不讨好——真实会话 49 次调用里误伤了 12 次，`git -C`、`npm --prefix`、`netstat -ano` 全被拒；而 `&` 之后的命令根本不经过校验，白名单本身就能被绕过。既挡住了不该挡的，又没挡住该挡的。

所以我把它整个翻转了：**默认放行，只硬拒不可逆且难以人工挽回的操作。** 威胁模型也写清楚——是「防模型手滑」，不是「防人类对手蓄意绕过」。这些数据和结论就写在那个文件的头部注释里，可以自己去看。

所以这是第三种模式。不是我写（`loop` 那种），也不是我抄一遍（`hook_core` 那种），而是**我定模型、agent 落实现**。

实际上这里**非常危险、非常危险、非常危险**，同时我也观察到，无论是一开始的白名单，还是后来的黑名单，其实很大程度上如果不限制agent写小脚本运行的能力，都是掩耳盗铃。

但其实这里我没有想出一个很好的不用user介入甄别的办法，因为我对powershell bash command其实并不了解；另一条路是每一个command都过一遍subagent做权限和危险性评估、再让用户提权，但这里其实也和权限模式掺和起来了，是更复杂的东西。

但是如果只是往prompt使劲，那只能说你这辈子有了，觉得prompt从上到下约束，各个地方重复就能保证肯定能拦住agent的行为，我想想啊...那很可能不如两眼一闭，小手一拍，坐那就开始祈祷没有人专心研究怎么破甲更好。

ps：没准祈祷祈祷可能更有效，毕竟唯心这一块，人家是专业的030。

所以，这里需要很严肃的进行安全方面的研究，或者沙箱隔离，沙箱这个东西是我又一个不了解的地方，时间太短，我也做不到面面俱到，我只能将所有的危险和需要注意的地方写出来。

### local_model 和 mcp_client：我一行没写

这两块是纯粹的外部接入。

`local_model/` 是 embedding worker，涉及独立进程、模型权重下载、进程间通信；`mcp_client/` 是接官方 MCP SDK，asyncio 那一套的坑（cancel scope 必须同一个 task 进出之类的）我到现在也没完全吃透。

这类活的特点是：**它的难点在别人的系统里，不在我的系统里。** 

我系统内的各种基础设施能支持无缝衔接，那就先用着，mcp其实目前做的不太符合我的想法，但是这里其实有点现实的原因，我得展示一下Alear030配合mcp的能力，所以就先让agent帮我安了一个进去，接入我的tool_core体系。

代价也很实在：这两块出问题的时候，我修得比别的地方慢，或者我就没法修，只能是bug驱动开发，让agent去修去。（哈哈哈哈哈哈，还讲什么测试驱动、需求驱动、用户驱动、资源驱动啊，小了，格局小了！我估计agent再普及普及，大家都得bug驱动了哈哈哈哈哈）

---

## 为什么能做到分工？

### 解耦

上面那张表能成立，靠的不是纪律。模块要是缠在一起，「这块这个习惯，那块另一种习惯」那就根本执行不下去——它改一个地方会波及别的地方，我还得回头去读它改了什么，然后规范一下，那还不如自己写。

所以真正让这套分工成为可能的是解耦。但解耦本身也不是我设计出来的，跟上面那些习惯一样，都是被问题逼出来的。

### 起因是循环引用

一开始我并没有这么解耦（我只是想代码写的优雅一点，虽然我也不知道什么是优雅的代码）。然后模块多了，彼此之间互相来回引用，然后就爆了循环引用的问题。

一开始我根本不理解为啥会这样。。。我不就是 import 了一下吗，为啥会出问题，后来我发现奥，这好像有点类似引用未声明变量，然后爆了虚拟内存的问题。

那时候我也没从整体的最顶层模块之间的框架去解决，甚至是拿懒加载顶着的——把 `import` 写到函数体里面去，用到的时候才导入，循环引用就绕过去了。

直到后来写 `loop` 那一块，耦合太深，一个循环引用的错误我追了好久，折磨。。。

追完之后我决定把它解出去。

当时其实还有一条更省事的路：继续用全局懒加载，所有出问题的地方都改成 `def` 里面 `import`，一样能跑通。但我觉得那样是不是就是不优雅啊？个人体感下来就是弄得烂七八糟的，我寻思就别了吧。（虽然当时我的agent跟我说，有很多现在成熟的python包也是这么做的，所以这里我只是说明我个人当时的想法，不能说明到底我的想法对不对，这是很个人的事情）

这个决定现在还能验证——整个项目里写在函数体内的 `import` 只剩 5 处，4 处在 `local_model/embedding_worker.py`（worker 子进程里延迟加载模型库那种重型依赖，是正当用途），1 处是个局部的 `import re`。**为绕循环引用而写的懒加载，一处都没剩下。**

### 解出去之后长出来的东西

- **三套注册机制**。Hook、Prompt、Tool 都靠装饰器注册 + import 期自动发现。落到派活上就是：给它一个新工具的任务，它只碰 `tool/tools/<名字>/` 这一个目录，三个文件，不用改 `loop`、不用改注册表、不用改 `main.py`。**改动面从一开始就是封闭的——它做错了，炸的范围也就那一个目录。**
- **引擎不做工具名分支**。工具分发全走字典查找，全仓库找不到任何一处 `tool_name == '某个工具'` 的硬编码判断。所以工具怎么加怎么改，都碰不到那台引擎。（`loop/orchestrator.py` 的提示词文本里确实写了 `plan_update`、`plan_mode_off` 这几个名字，但那是给模型看的话，不是代码里的分支。）
- **plan 编排整块剥出去**。`loop/loop_core.py` 里关于 plan 只剩一行委托，多轮驱动、卡死熔断、step 提示词拼装全在 `loop/orchestrator.py`。
- **门面收窄**。每个模块对外只暴露几个名字。

### 门面窄到什么程度，就敢交出去多少

这条是整件事里最能说明问题的：

| 模块 | 规模 | 对外暴露 |
|------|------|----------|
| `local_model/` | 4 文件 / 611 行 | 8 个名字 |
| `mcp_client/` | 5 文件 / 648 行 | 3 个名字 |

`main.py` 里关于这两个模块，总共四行：启动的时候 `prewarm`，退出的时候 `shutdown`。

**我一行都没写的那两块，恰恰是门面最窄的两块。这不是巧合。** 契约窄到只剩三个函数，我就不需要知道里面是怎么实现的，只需要知道它什么时候起、什么时候停。顺序是先有解耦，才敢整包交出去。

`tui/` 也是同一个道理：它对 `loop`、`session`、`memory` 一行字面 `import` 都没有，全是构造时注入进来的实例，然后只调那么几个公开方法。

### 代价：解耦掉的是 import，不是耦合

这一节我得往回说说，因为上面听起来太漂亮了，但是实际上如果你追代码，其实能发现，耦合只是从表面上转移到了更深的地方，所以如果你想写一个tool、prompt甚至是hook扩展还是比较容易的，但如果你想动更核心一点的地方，那你就享福了。

依赖图大体是干净的——`memory`、`hook`、`tui` 这三个顶层包没有被包外的其他生产模块直接 import 过，统一由 `main.py` 组装。

**耦合并没有消失，它换了个地方藏**。因为所有运行时对象都是注入进去的，模块之间不用 `import` 也能互相依赖，而这种依赖静态分析看不出来：

- `hook` 里的后台钩子直接调 `session._session_slice()`——一个带下划线的私有方法，还直接解析 session 落盘 JSON 的内部字段结构，三个钩子重复了三遍
- `memory` 直接改写 `memory_agent.message_list`，也就是 Agent 的内部状态
- `tool_core` 调 `hooks.trigger(...)`，但 `tool/` 目录里没有任何一行 `import hook`
- `loop` 驱动工具执行，而工具又反过来 `import Loop` 去造新实例——运行时这是一个真实的双向依赖，只是没用 import 语句连起来

最要命的一点是：**这种靠约定维持的耦合，约定被违反的时候没有任何东西会报错。**

很神奇的效果是吧哈哈哈哈，代码逻辑可能有问题，但就是能运行，直到这个问题被触发到。。。然后艺术就是派大星！！！

举个栗子。我在 `memory_core.py` 里给自己写过一条红线注释，大意是不要在 tool 侧另开一条直接读取的平行路径，memory 的读取要统一收口在 Memory 类里。结果 `memory_recall` 这个工具就是绕开注入的 memory 对象、自己去读 session 文件并解析切片字段的——红线写在 memory 那边，违反发生在 tool 这边，中间没有 import 关系，所以谁都没拦住。程序跑得好好的。ps：another tech debt....

类似的还有几处：三套自动发现的 `import` 都没有异常兜底，随便哪个插件文件坏掉就能炸掉整个启动；两个注册表遇到重名是静默覆盖，不吭一声。这些我都记成 issue 了，慢慢还。

顺带说一句，`memory` 那 900 多行的 core 到现在还没拆重构。没什么深层原因，就是还没做...嘻嘻

---

## 用了哪些 agent

### hermes

早期的老师，前面说过了。它是我从「完全不会」走到「能自己写」这段路上的指引。

陪我度过了一段不错的时间。

### Claude Code

现在的主力。`CLAUDE.md` 和 [`.claude/skills/` 下那十一个技能](.claude/skills/README.md)都是给它的。跨模块的机制改动、对外文档、CHANGELOG、commit message 基本走它。

用它主要是因为他是目前唯一一个（我使用过的）能在edit file之前给我看diff，然后让我approve的agent，其他的都是让我看那个雷霆大review，我就说白了我白说了，那个雷霆review谁愿意看啊，而且一个地方有问题，其他的地方留不留？啧...

不过后来我尝试cursor一段时间之后，发现其实也不是不能适应，只是边角料这样就算了，核心的地方，每一行都得经过我的授权才能落盘，哪怕只是我懒得写了让他们去写，我也得控制控制这些agent！

### Cursor

`.cursor/rules/` 下那五条规则是给它的。

tab自动补全有点东西，multitask有点东西，做的挺好挺方便的，plan的那个user_view界面做的不错，里面的todo_list我喜欢，总体是个不错的IDE，但是能不能完全替代vscode呢？

如果是重度依赖自动补全，其实可以，但如果不是，那就悬了，因为也不知道是我电脑配置不太好，还是因为什么，我发现就算我把自动补全关上了之后，IDE自带的那个补全提示，体感也非常慢，或者应该说是慢到你能明显感受到你需要等他一段时间，然后思路打断的感觉，离谱。

可能是vscode之上做自动补全逻辑屏蔽或者above上去了吧，不知道。

### Codex

代码里的 `@codex` 标记集中在 TUI 那一段时间。

不适合作为code buddy，适合定一个目标就给我跑去吧，我平时PM工作会比较常使用他，效果不错。

### Alear030 自己

这条得单独说清楚，不然容易混。

我说的「Alear030 自己参与开发」是另一回事：

并非是他自己写自己的代码，类似cc的那种dogfood的迭代，因为有一点就是我知道Alear030能做事情，但是杀伤力非常大，而且我没有足够的工程能力底气能给他兜底。

（就这里不得不提我那个eval的worktree搞没了的事情，我真牛逼啊，服了，当时还没觉得这玩应咋样，就是看看我的memory搞出来之后，能不能用，没想到这东西这么重要。）

所以Alear030目前编辑文件的能力还是多重限制（prompt引导+工具硬路径判断），只有当权限模式已经适配上文件编写TUIdiff展示和approve能力之后，我才会让他对自己进行迭代和开发，这也是我最初定下的计划。

但是这并非是Alear030没有帮助我开发，相反他在我的想法梳理上帮助我很多，我跟他在最开始的对话记录应该是6月10几号开始，但是初期的时候我没有memory的意识，所以没有留下语料，真的非常可惜，但是后续留下来的对话记录，基本上都是我给他新写了一些东西，然后我让他自己看看，听听他的想法和观点，同时辅助我进行思考和方案的调研。

ps：这里提一嘴Alear030的雷霆大plan，每一次plan执行几乎都得闷头跑个几十分钟，我有时候真不明白这小子到底怎么想的，所以一直想做一个todo_list轻一点的东西。

然后我也在权衡，目前的这个plan状态机，要不要留下来。其实有这个状态机很大程度上是对模型自身的约束性过度的不信任，以及同形态能力的缺失。

其实一个plan.md一个todo_list，attachment里面增加轮次提醒和使用提醒，就能很轻量化的解决这个雷霆状态机的问题，所以后续我应该也会重构plan这里的东西，可能会放到一个别的地方作为重型机器使用。

然后就是我一边写，一边讨论怎么写。[记忆的想法&构思](docs/design/memory.md)那一整篇就是这么来的——那篇文章的素材是从项目自己的历史会话记录里挖出来的，61 个会话、393 个切片。**用这套记忆系统的产物，重建它自己的设计史。**

这大概是这个项目最有意思的一点，也是我觉得agent类型项目对个人开发者吸引力非常大的一点，可能科技再发展发展到时候大家学面向对象编程可能真的就是面向对象编程了哈哈哈哈哈哈

---

## 代码里那些 @claude / @codex / @cursor 标记

读代码会撞见这种东西：

```python
self.length_end = False# @claude 后续需要添加流式长度，截断工具执行的逻辑
```

这是我留给 agent 的待办标记。写代码的时候想到一件后面要做的事，但当下不想被它打断，或者这里的逻辑我懒得写了，就地留一个标记，然后复活吧！我的agent！

三条约定：

- `@claude` / `@codex` / `@cursor` —— 给对应 agent 的任务
- `# done(@claude): 做了什么` —— 完成之后原地改写，痕迹留着，也不会被下次扫描重复捡起来
- `@claude(ignore)` —— 我给自己的备注，不是任务，别动

为什么不用 `TODO`？因为 TODO 是给人看的，看不看随缘。这些标记是**有消费者**的——有一个专门的技能负责把它们扫出来、列成清单、逐条问我要不要做。差别在这儿。ps：以上的理由是我的agent给我写的，实际上就是我没想到todo，我想的很简单，我让你整那我就直接@你就完了呗。

不过得说明：仓库里没有配自动扫描，得我主动喊一声才扫。想让它每次会话开始自动跑，需要自己配一个 SessionStart hook。

---

## 规约文件：为什么这些文件这么重要？

仓库里三份东西是给 agent 使用或者默认加载到context里面的

- `CLAUDE.md` —— 项目概述、稳定模块地图、核心运行数据流、协作方式、数据安全红线
- `.cursor/rules/` —— 5 条规则
- `.claude/skills/` —— 11 个技能，逐个说明见[技能目录](.claude/skills/README.md)

我把它们放进版本控制，不是为了能让人类进去研究，没有必要。而是因为agent的context很珍贵，不能让他每次都是白板进去，写一个小东西就要探索整个仓库，那就太。。。怎么说呢。。。太富有了，佩服！

**所以需要将一些仓库规约、结构、框架、合作方式，能固定的都高度凝练固定下来，默认加载进去**

ps：context管理，cache管理这俩哼哈二将，我觉得可能在一个agent项目里，只有引擎比这俩重要了。

而且这肯定不只是model侧需要努力的东西，毕竟model侧只是接收你传过去的数据，然后给你来个雷霆大计算，返回结果给你，他能将你传过去的东西计算过的缓存下来，但是不能保证你怎么传给他。

Alear030里涉及到了一部分，但是目前有的东西不足以让我单独写下来，所以后续再说，model相关实际上我懂得不多，其实不应该这样。。。

同时这里我比较推荐cc的邪恶管家他们的那些研究，就是大家都知道。。。但是也不能。。。对吧，不说太多，sycjyzy，懂的都懂030

回到正题，里面实际上更值得说的是：**这些规矩没有一条是我坐下来设计出来的，全是踩出来的。** 每一条背后都有一次它做错了的经历。

举三个。

**「外科手术式改动」** —— 改动范围由机制的整体收口需求界定，不按任务字面最小化。

这条是反着来的。一开始我以为「最小改动」是美德，结果发现它会留下一堆半吊子：改了生产者没改消费者，加了新路径没删旧入口。所以现在写的是：修复牵涉调用链的时候要主动扩大探索，把机制级的连带项列进方案；任务字面不足以达成目标时显式提出超范围项交我拍板——**提出不算越界，回避才算越界。**

**「临时截断 ≠ 删除实现」** —— `.cursor/rules/minimal-disable-preserve-body.mdc`

这条特别具体，因为它特别疼。我说「先把这个功能关掉」，结果是函数体被删空了只留一个 `return`。关掉和删掉在字面上很接近，但它们是两件完全不同的事。这条规则里那个 `rich_print` 的正反例就是当时的现场，我没有把它改成抽象示例，留着当纪念。规则最后那句自检是重点：**「用户若去掉这一行 return，原功能能否原样恢复？」**

**「同一问题纠正超过两次 → 停下重开」**

这条是给我自己定的，不是给它定的。同一个地方我纠正两次还没对，说明不是它没听懂，是我没说清楚——继续叠补丁只会越描越黑。这时候该做的是停下来，换根本思路，或者把已知信息重写成更清楚的需求。

还有一整套是关于数据安全的：`session/session_detail/`、`memory/memory_storage/` 这些是真实的会话与记忆数据，不是能随便重建的临时文件。规矩写得很硬——未经我明确授权禁止删除、清空或批量覆盖；需要干净环境验证时用临时目录或临时 session id，不许清场式测试真实数据；用真实历史数据重放验证的时候（比如测试新版 prompt），测试前后要对相关文件算 MD5 比对，用来证明测试脚本没有意外写入。

---

## 关于技能

ps：我之前经历过追捧skill的浪潮，甚至最开始的时候我也做过一套雷霆PM SKILL，现在回头看，两件事情，适合别人的不一定适合你、AI赋能是乘法而非绝对的加法

仓库里技能这个东西，有两套存储的位置。

一边是 `.claude/skills/`，我写给 coding agent 的十一个技能。另一边是 `skill/`，Alear030 自己的运行时技能系统。

**格式是同一套。** 两边都是一个目录一个 `.md` 文件，都是 YAML frontmatter 里写 `name` 和 `description`，后面跟 Markdown 正文。触发也都主要靠 `description` 匹配——读的那一方看描述自己判断这次该不该用，而不是我在流程里写死什么时候调它。

**加载路径也同构。** Alear030 这边是 `prompt/prompts/skill_prompt/prompt.py` 在 Agent 构造的时候，把磁盘上全部技能的 `name` 和 `description` 拼进 system prompt（order 20，而且只给持有 `skill_tool` 授权的 agent），模型看到这张列表之后自己决定要不要调 `skill_load` 把正文读进来。coding agent 那边也是先看 description、命中了才加载全文。是同一个机制。

**区别在于谁来沉淀。**

有些是主动create-skill产出的：踩一次坑、重复一件事情，就create出来一个。十一个技能 1198 行，全是这么攒出来的。

Alear030 这边自动的（自动提议产出的目前就一个）。memory 管线在同类任务累积到阈值的时候会产出技能候选，以 attachment 的形式打断当前对话：这类事已经做过几次了，建议固化成一个可复用技能。然后走 `create-skill` 起草、我确认、落盘，最后 `skill_finish` 回写任务节点并把累积计数清零，免得下次重复提示同一件事。

写到这儿我才反应过来，这两件事其实是同一件。我在[记忆的想法&构思](docs/design/memory.md)里总结过一句话——**「让数据产生结构，让使用驱动优化」**。技能就是这句话在最表层的样子吧。

---

## 协作的节奏

默认流程是四段：**探索 → 规划 → 执行 → 验收**。

1. **探索** —— 先读代码搞清现状，不改任何东西，产出「现状是 X」
2. **规划** —— 提方案，主动点出取舍和风险，产出我能改、能拍板的计划
3. **执行** —— 我拍板之后才动手
4. **验收** —— 给测试输出或者跑通结果，不靠「做好了」这句断言

只有**无行为影响的纯文本改动**（错字、注释、我明确指定的重命名）可以跳过前两段直接做。碰行为、碰机制、碰参数、碰调用链的，哪怕一句话说得清，也得先过规划这道闸门。

为什么不走「我给需求 → 它闷头执行」？因为我给的需求经常是错的。不是表述错，是我自己还没想清楚。探索这一段的真正价值不是让它了解代码，是让我在看它的现状汇报时意识到「哦原来现在是这样，那我想要的其实是另一个东西」。

项目体量上来之后又在这上面叠了几条：

- **切片化工作单元** —— 跨模块的大改动按「可独立提交、可运行」切开，一次会话一个闭环，不留带已知缺陷的半成品
- **验证优先** —— 机制级改动先说清楚「怎么快速验」再动手，探针尽量固化进 `test/` 而不是随用随删
- **并行编排** —— 跨模块改动派多个子代理并行探索、并行审查；闸门分三级：纯文本直接做 / 单模块机制自验 / 跨模块走完整流程

分工边界我写在 `CLAUDE.md` 里，大意是：方向、品味、北极星判断和「什么算够好」的最终拍板归我；读代码、查资料、铺上下文、起草方案、实现、验证归它。提出方向、上浮机制根因、指出跨模块影响是它的职责——**提出不算越界，拍板才算。**

另外还有一条我自己觉得挺关键的：**模糊提问是合法的，而且是被鼓励的。** 开放问题用来共创，不要求我每次都先给出一个成型的需求。很多时候我就是带着一句「我感觉这里怪怪的」开的头。

---

## 我怎么确认它写的东西是对的

这是整套协作里最容易被忽略、但决定成败的一环。交出去的活如果验不了，那不叫协作，叫祈祷。

我用的是三层，从重到轻：

**抄一遍。** 只对关键路径用，`hook_core` 就是这么过的。

**跑验证。** 这个项目的验证方式有几个反直觉的坑，我专门写成了技能：`python main.py` 在主仓库不是无副作用的冒烟测试（它会写 session 文件、会调模型 API），验证脚本必须用 `python -m` 点号路径调用，`unittest discover` 不能带 `-s test` 参数。这些都是踩过之后才写下来的。

**看差异。** 改动落盘之后 `git diff` 自己过一遍。这层最轻，但能抓住「它顺手改了别的地方」这类问题。

得说实话：**这套东西不是万无一失的。**

我到现在还没有一个留存的并且是专项完整的 eval 题集（golden set 还在 Roadmap 上没做），也就是说记忆系统召回效果这种「不会报错但会慢慢变差」的东西，我目前只能靠体感判断。

---

## 所以该怎么看

两方面吧：

**架构判断、取舍和方向是我的。** 核心框架是我一行行写的，那些看起来奇怪的设计（强制收尾靠不传 tools 物理断供而不是写提示词、mode 切换靠前后 diff 而不是信任模型自觉、MCP 的 asyncio 封死在单个常驻 task 里）都是我拍的板。

**具体落笔有相当一部分不是我敲的。** 外部接入基本没经我手，边角料和机械的数据处理也交出去了。

这两句不矛盾，后一句我也不觉得需要藏着，因为其实就是这样。

最后说一句：这套分工是我自己撞出来的，不一定对，也不一定适合你。但它是真的，每一条边界背后都有一次具体的疼。如果你也在纠结「哪些该自己写、哪些能交出去」，我的答案是——**听从你的内心，当你觉得这个部分似乎应该自己写，那就先自己尝试吧，大概率会有收获**

---

← [返回 README](README.md) · [参与方式](CONTRIBUTING.md) · [记忆的想法&构思](docs/design/memory.md)
