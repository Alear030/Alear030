/* 全局：内容区吃剩余高度；输入条样式在 UserInput/widget_css.tcss */

Screen {
    background: transparent !important;
}

/* channel 滚动区：与 dock bottom 的输入条分高 */
#main_CHANNEL_BODY {
    height: 1fr;
    scrollbar-size: 0 0;
    background: transparent !important;
}

