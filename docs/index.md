# 文档目录

**中文** · [English](index.en.md)

← [返回 README](../README.md)

这是 `docs/` 的入口页，不是仓库首页——项目介绍和快速上手见根目录 [README](../README.md)。

## 体裁

`docs/` 下的文档分两类：**四级递进**讲这个项目自己，**一类旁支**讲项目外部的东西。新增文档先归位再动笔。

### 四级递进（对象是 Alear030 自己）

- **总览文档**（`docs/` 根）：跨模块的整体说明，面向第一次读这个项目的人
- **模块机制文档**（`docs/modules/<module>.md`）：单个模块「现在是什么样」，文件名对齐 [架构文档](ARCHITECTURE.md) 「目录结构」里的模块名
- **设计叙事文档**（`docs/design/<topic>.md`）：单个机制「为什么长成这样」，和机制文档解耦
- **研究文档**（`docs/research/<topic>.md`）：一个**还没有定论**的问题——假设、实测数据、结论，以及被推翻的假设和撤回的结论

前三层讲的都是已经做成的东西，研究文档讲的是方向性或者还没落盘或者长期跟踪记录的东西，所以它有两个不一样的地方：**允许没有结论**，以及**保留出错的过程**。推翻自己的假设、撤回基于错误方法得出的结论，都留在文里而不是删掉重写——一条结论怎么来的，和结论本身一样重要——所以如果真有人认真查看了这里面的文档，可能需要有自己的判断或者多一点质疑。

研究出了结论之后，要么催生一条改动型 issue 去落地，要么沉淀进对应的设计叙事或模块机制文档；研究文档本身不承担「当前实现是什么样」的说明责任。

### 旁支：观察文档（对象是外部系统）

- **观察文档**（`docs/observations/<topic>.md`）：**别人家的系统，怎么解一个我也在解的问题**

它不是「第五级」。前四级共享同一条轴——对象都是本项目，按「是什么 → 为什么 → 还没定论」递进；观察换掉的是**对象**，不是确定性档位。

准入判据是三条全中：①对象在项目外部；②有本机可复现的一手证据，不是聊天里的转述；③有对照价值，即它解的是我也在解的问题。只是有趣、但没有对照价值的见闻不进 `docs/`。

它还有一条前四级都没有的约束：**没有「以代码为准」可以兜底**——证据不在本仓库里，而且被观察方会自己改。替代的兜底是三件事：标明观察日期与被观察版本、把「怎么观察到的」写进正文让人能自己复现、给每条断言标注证据等级——一手（本机文件或可复现行为）／二手（被观察系统自己的说法，**这不构成验证**）／三手（推断）。

和研究文档一样，允许没有结论、保留猜错的过程。区别在出口：研究出了结论要落地成 issue 或沉淀进别的文档，**观察只记录、不立项**——观察若催生了自家改动，那条改动自己走 issue 或设计叙事，观察文不承担。

只有已经写出实质内容的文档才会出现在下面的列表里；尚未成文的模块不预留占位链接。

## 总览

- [架构文档](ARCHITECTURE.md) —— 目录树、启动/退出流程、各模块职责、核心设计决策
- [配置说明](CONFIGURATION.md) —— `.env`、MCP 接入、运行常量
- [扩展指南](EXTENDING.md) —— 怎么加一个工具/Hook/Prompt/技能

## 模块机制

- [记忆系统](modules/memory.md) —— 切片、分类、去重、画像、跨会话时间线、语义召回的完整机制
- [MCP 客户端](modules/mcp_client.md) —— 四层结构、asyncio 隔离与 cancel scope 约束、工具表运行时刷新、凭证占位符
- [TUI](modules/tui.md) —— 事件流、线程模型、widget 与事件的两套注册体系、Textual 踩坑

## 设计叙事

- [记忆的想法&构思](design/memory.md) —— 记忆系统为什么长成现在这样
- [Loop 的想法&取舍](design/loop.md) —— 为什么 Loop 不知道 plan 存在，以及强制收尾、mode diff、错误边界、流式累积四个决定

## 研究

- [LLM Cache 方向研究&现象观察](research/llm-cache.md) —— *进行中·累积式* · 当前主线是 prompt cache 的跨 session 复用：为什么新 session 首轮只有 14% 命中率；证伪了最初的假设，定位到 12.7K 工具 schema 被一句时间戳连累，并撤回了一条基于错误分词器得出的结论

研究议题在 GitHub 上用 [`research` 标签](https://github.com/Alear030/Alear030/issues?q=is%3Aissue+label%3Aresearch)跟踪，里面会有一些初期的文档发现、问题探查、阶段性结论之类的历史记录。

## 观察

- [ZCode 的记忆系统观察](observations/zcode-memory.md) —— *累积式·观察于 2026-09-03* · 从文件结构逆向一套跨会话记忆机制：三处和自己同构的选择、两处我猜错的地方，以及一个设计者自己没在规则里设防的缺口——过时结论沿着自动加载的索引静默传播
